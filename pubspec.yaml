
name: urban_eye_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.4  # For camera functionality

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/images/urban_eye_logo.jpg

  uses-material-design: true